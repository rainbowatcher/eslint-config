import type { StyleOptions } from "./types"

export const DEFAULT_STYLE_OPTION: Required<StyleOptions> = {
    semi: false,
    singleQuote: false,
    tabWidth: 4,
    trailingComma: "all",
    useTabs: false,
}
